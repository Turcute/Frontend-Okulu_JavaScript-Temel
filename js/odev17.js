// Görev Listesinden Son Elemanı Silme (pop)

let tasks = ["Mail gönder", "Toplantıya katıl", "Kod review yap"];

console.log(`All Tasks: ${tasks}`)

let deletedTasks = tasks.pop();

console.log(`Deleted tasks: ${deletedTasks}`)
