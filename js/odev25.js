// Bir Dizinin Belirli Bir Aralığını Kopyalama (slice)

let lessons = ["JavaScript", "HTML", "CSS", "React"];

console.log(`Lessons: ${lessons}`);

let newLessons = lessons.slice(0,2)

console.log(`New Lessons: ${newLessons}`);